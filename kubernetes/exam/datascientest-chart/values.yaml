replicaCount: 1

image:
  repository: YOUR_DOCKERHUB_USERNAME/fastapi-app
  tag: "1.0.0"
  pullPolicy: IfNotPresent

database:
  password: datascientest1234

service:
  type: ClusterIP
  port: 8000

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 80

ingress:
  enabled: false
  hosts: []
